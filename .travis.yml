language: java
jdk:
  - oraclejdk7
  - openjdk7
  - openjdk6
before_install:
  - chmod +x gradlew
  - sudo apt-get install gradle
install:
  - true
before_script:
  - TERM=dumb ./gradlew setupCIWorkspace
script: TERM=dumb ./gradlew buildNeeded
